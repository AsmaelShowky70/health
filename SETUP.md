# 🚀 دليل التشغيل والاستخدام

## ✅ حالة المشروع الحالية

تم تشغيل المشروع بنجاح وجميع المكونات تعمل بكفاءة!

### 📱 المخدومات النشطة

| المكون | المنفذ | الحالة | الرابط |
|-------|--------|-------|--------|
| **Frontend (React)** | 3000 | ✅ يعمل | http://localhost:3000 |
| **Backend (Node.js)** | 5000 | ✅ يعمل | http://localhost:5000 |

> في حال استخدام النسخة المنشورة من الواجهة الأمامية اضبط المتغير البيئي `REACT_APP_API_URL` ليشير إلى https://helth-api.vercel.app/api (الافتراضي أيضًا في الكود).
| **Database (Neon PostgreSQL)** | - | ✅ متصلة | AWS/Neon Cloud |

### 🔧 المشاكل التي تم حلها:
- ✅ **Cannot GET /** - تم إضافة root endpoint
- ✅ **خطأ تسجيل دخول المسؤول** - تم إنشاء حساب المسؤول في قاعدة البيانات
- ✅ **JWT Secret ضعيف** - تم تحديثه بقيمة آمنة
- ✅ **مشاكل المصادقة** - تم إصلاح الـ auth controller

👉 **للمزيد من التفاصيل، راجع** [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)

## 🗄️ قاعدة البيانات

### الاتصال
- **نوع السيرفر**: PostgreSQL (Neon)
- **المضيف**: ep-wild-water-aiunjfnc-pooler.c-4.us-east-1.aws.neon.tech
- **الاسم**: neondb
- **المستخدم**: neondb_owner
- **الحالة**: ✅ متصلة وجاهزة

### الجداول المنشأة
1. ✅ **users** - بيانات المستخدمين والحسابات
2. ✅ **articles** - المقالات الطبية
3. ✅ **tips** - النصائح الصحية
4. ✅ **consultations** - الاستشارات الطبية
5. ✅ **calorie_calculations** - نتائج حاسبة السعرات
6. ✅ **diagnoses** - نتائج تشخيص الأعراض

## 🔐 حسابات الدخول الافتراضية

### حساب المسؤول (Admin)
```
البريد الإلكتروني: asmael@gmail.com
كلمة المرور: Asmel010@#
رابط الدخول: http://localhost:3000/admin-login
```
أدخل البريد: admin@health.com
أدخل كلمة المرور: Admin123

### حساب تجريبي (User)
يمكنك إنشاء حساب جديد من:
```
http://localhost:3000/register
```

## 📝 الميزات المتاحة

### للمستخدمين العاديين
- 📚 قراءة المقالات الطبية
- 💡 الحصول على النصائح الصحية
- 🩺 طلب استشارة طبية
- ⚖️ حساب السعرات الحرارية
- 🔍 مشخص الأعراض الذكي
- 👤 إنشاء حساب شخصي
- 💬 تتبع الاستشارات الطبية

### للمسؤولين
- 📖 إضافة وتعديل وحذف المقالات
- 💡 إدارة النصائح الطبية
- 💬 الرد على الاستشارات الطبية
- 👥 إدارة حسابات المستخدمين
- 📊 عرض الإحصائيات
- 📋 تتبع الأنشطة

## 🌐 واجهات الويب الرئيسية

### الصفحات العامة
- **الرئيسية**: http://localhost:3000
- **المقالات**: http://localhost:3000/articles
- **النصائح**: http://localhost:3000/tips
- **الاستشارة**: http://localhost:3000/consultation
- **حاسبة السعرات**: http://localhost:3000/calorie
- **مشخص الأعراض**: http://localhost:3000/diagnosis
- **تسجيل دخول**: http://localhost:3000/login
- **إنشاء حساب**: http://localhost:3000/register

### صفحات الإدارة
- **لوحة التحكم**: http://localhost:3000/admin
- **إدارة المقالات**: http://localhost:3000/admin/articles
- **إدارة النصائح**: http://localhost:3000/admin/tips
- **إدارة الاستشارات**: http://localhost:3000/admin/consultations
- **إدارة المستخدمين**: http://localhost:3000/admin/users

## 🔌 REST API Endpoints

### المصادقة (Authentication)
```
POST /api/auth/register        - إنشاء حساب جديد
POST /api/auth/login           - تسجيل دخول مستخدم عادي
POST /api/auth/login-admin     - تسجيل دخول مسؤول
```

### المقالات (Articles)
```
GET    /api/articles           - الحصول على جميع المقالات
GET    /api/articles/:id       - الحصول على مقالة واحدة
POST   /api/articles           - إضافة مقالة (مسؤول فقط)
PUT    /api/articles/:id       - تعديل مقالة (مسؤول فقط)
DELETE /api/articles/:id       - حذف مقالة (مسؤول فقط)
```

### النصائح (Tips)
```
GET    /api/tips               - الحصول على جميع النصائح
GET    /api/tips/:id           - الحصول على نصيحة واحدة
POST   /api/tips               - إضافة نصيحة (مسؤول فقط)
PUT    /api/tips/:id           - تعديل نصيحة (مسؤول فقط)
DELETE /api/tips/:id           - حذف نصيحة (مسؤول فقط)
```

### الاستشارات (Consultations)
```
POST   /api/consultations           - إنشاء استشارة جديدة
GET    /api/consultations           - الحصول على جميع الاستشارات (مسؤول)
GET    /api/consultations/:id       - الحصول على استشارة واحدة
GET    /api/consultations/my-consultations - الحصول على استشاراتي
PUT    /api/consultations/:id/reply - الرد على استشارة (مسؤول)
PUT    /api/consultations/:id/status- تحديث حالة الاستشارة (مسؤول)
```

### حاسبة السعرات (Calorie)
```
POST   /api/calorie/calculate   - حساب السعرات
POST   /api/calorie/save         - حفظ النتيجة (مستخدم)
GET    /api/calorie/history      - السجل (مستخدم)
```

### التشخيص (Diagnosis)
```
POST   /api/diagnosis/diagnose   - تشخيص الأعراض
POST   /api/diagnosis/save       - حفظ التشخيص (مستخدم)
GET    /api/diagnosis/history    - السجل (مستخدم)
```

### الإدارة (Admin)
```
GET    /api/admin/dashboard         - إحصائيات لوحة التحكم
GET    /api/admin/users             - قائمة جميع المستخدمين
DELETE /api/admin/users/:id         - حذف مستخدم
GET    /api/admin/consultations/pending - الاستشارات المعلقة
GET    /api/admin/logs              - السجلات
```

## 🛠️ أوامر مفيدة

### تشغيل المشروع
```bash
# تشغيل الخادم فقط
cd server
npm run dev

# تشغيل العميل فقط
cd client
npm start

# تشغيل كليهما
npm run dev  (من المجلد الرئيسي)
```

### تهيئة قاعدة البيانات
```bash
cd server
node initDb.js
```

### إصلاح الثغرات الأمنية
```bash
npm audit fix --force
```

## 📊 مثال على طلب API

### إضافة مقالة جديدة
```bash
curl -X POST http://localhost:5000/api/articles \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "أهمية شرب الماء",
    "content": "الماء ضروري لصحة الجسم...",
    "category": "تغذية",
    "image_url": "https://example.com/image.jpg"
  }'
```

### إنشاء استشارة طبية
```bash
curl -X POST http://localhost:5000/api/consultations \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "ألم في الرأس",
    "description": "أشعر بألم مستمر في الرأس...",
    "age": 30,
    "gender": "male",
    "symptoms": "صداع، دوخة"
  }'
```

## ⚠️ ملاحظات مهمة

1. ✅ **قاعدة البيانات**: محمية بكلمة مرور قوية وتحديثات SSL
2. ✅ **المصادقة**: جميع كلمات المرور مشفرة بـ bcryptjs
3. ✅ **التوكنات**: توكنات JWT آمنة مع صلاحية 7 أيام
4. ⚠️ **الخصوصية**: المحتوى الطبي لأغراض تعليمية فقط
5. ⚠️ **الاستشارات**: لا تحل محل استشارة الطبيب المختص

## 🆘 استكشاف الأخطاء

### المشكلة: الخادم لا يبدأ
**الحل**: 
- تأكد من تثبيت Node.js
- امسح `node_modules` وأعد التثبيت: `npm install`

### المشكلة: لا يمكن الاتصال بقاعدة البيانات
**الحل**:
- تحقق من متغيرات `.env`
- تأكد من الاتصال بالإنترنت
- تحقق من بيانات الوصول على Neon

### المشكلة: المنفذ 3000 أو 5000 قيد الاستخدام
**الحل**:
```bash
# Windows
netstat -ano | findstr :3000  # ابحث عن العملية
taskkill /PID <process_id> /F  # اقتل العملية

# macOS/Linux
lsof -i :3000
kill -9 <process_id>
```

## 📞 الدعم والمساعدة

للمزيد من المعلومات، راجع:
- 📖 [README.md](./README.md) - التوثيق الشامل
- 📝 [schema.sql](./database/schema.sql) - هيكل قاعدة البيانات
- 🔧 [.env.example](./server/.env) - متغيرات البيئة

---

**آخر تحديث**: فبراير 2026
**الحالة**: ✅ المشروع يعمل بنجاح تام
